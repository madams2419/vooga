package authoring.panes.centerPane.modes;

import java.util.List;
import javafx.scene.Group;
import javafx.scene.Scene;
import javafx.scene.input.MouseEvent;
import authoring.dataEditors.Sprite;
import authoring.panes.centerPane.Region;


public class PathEditorMode extends Mode {
    @Override
    public void canvasClicked (MouseEvent e,
                        Region myCurrentRectangle,
                        Scene myScene,
                        Group myGroup,
                        List<Sprite> listOfSprites) {
        
    }

}
